model User {
  id            String    @id
  name          String
  email         String    @unique
  emailVerified Boolean
  image         String?
  username      String?   @unique
  dob           String?
  createdAt     DateTime
  updatedAt     DateTime
  sessions      Session[]
  accounts      Account[]
}
